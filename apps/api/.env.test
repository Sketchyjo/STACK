# Test Environment Configuration
NODE_ENV=test
LOG_LEVEL=error
PORT=3002

# Test Database (use in-memory or test DB)
DATABASE_URL=postgresql://test:test@localhost:5432/stack_test

# Test JWT Secret
JWT_SECRET=test-jwt-secret-key-for-testing-only-32-chars
JWT_EXPIRES_IN=1h

# Test Turnkey Configuration (all fake values for testing)
TURNKEY_BASE_URL=https://api.turnkey.com
TURNKEY_ORG_ID=test-org-id
TURNKEY_API_KEY=test-api-key
TURNKEY_PRIVATE_KEY=test-private-key

# Feature Flags
FEATURE_WALLET_APTOS_ENABLED=true
FEATURE_WALLET_SOLANA_ENABLED=true
FEATURE_WALLET_EVM_ENABLED=true

# Rate Limiting (more lenient for tests)
RATE_LIMIT_MAX=1000
RATE_LIMIT_WINDOW=1 minute